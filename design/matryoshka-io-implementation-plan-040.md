# Matryoshka Zig — Implementation Plan (040)

Replaces [matryoshka-io-implementation-plan-039.md](matryoshka-io-implementation-plan-039.md).

## Status

DOC 20 — DONE (167/167 tests). Removed the 8 example-autodoc `zig build docs`  
targets; added a hand-organized examples catalog under `kitchen/docs/examples/`,  
generated by a new permanent `kitchen/tools/gen_examples_docs.sh`.

---

## Completed stages (summary)

- Stage 0–8: API, tests, examples layers 1–3 done.
- Stage 9 (Layer 4 infrastructure): pool, mailbox, select, group done.
- EXMPL 3a: 45 layer4 examples written (layer4.zig registration, test wrappers).
- EXMPL 3b: renamed all layer4 examples; Master pattern applied to 6 complex files.
- EXMPL 3c: Observable by human rule (rules-005 → rules-006); fixed 3 Master violations.
- EXMPL 3d: extracted step functions from 31 flat layer4 files.
- EXMPL 3e: structural extraction signals (rules-007); fixed 24 Observable violations; patterns-006 → -007.
- API 2: renamed `cast`→`identifyNodeAs`, `mustCast`→`mustIdentifyNodeAs`; added `identifySlotAs`/`mustIdentifySlotAs`. api-reference-015 → -016, patterns-006 → -007, rules-007 → -008. 161/161 tests.
- EXMPL 4: "Description as code" rule (rules-008 → -009); staccato descriptions moved into `///` doc comments; layer1–3 renamed with `NNN-` prefix; layer4's 47 files rewritten to match. patterns-007 → -008. 161/161 tests.
- EXMPL 4b: descriptive entry-point names rule (rules-009 → -010); every example's `pub fn run` renamed to `pub fn @"<description>"`; ~66 test-wrapper call sites updated. 161/161 tests.
- API 3: added `mailbox.wakeUpAll()`; new `error.Wakeup` receive arm; new example 097-wake_up_all.zig. api-reference-016 → -017, patterns-008 → -009. 167/167 tests.
- DOC 9: re-partitioned api-reference-017 → -018 into teachable order; generic `std.Io` material moved to new Addendums/Io 101 section; dropped 16 stale Change-manifest sections. Doc-only.
- DOC 10: dependency-ordered api-reference-018 → -019 (send/receive diagrams into mailbox, Tag identity after pool, Slot/cleanup patterns after pool). Doc-only.
- DOC 11: wrote matryoshka-manifesto-002.md, consolidating README + model + master docs into one persuasion-first manifesto. docs-plan-008 → -009. Doc-only.
- DOC 12: de-smarted the manifesto (-002 → -003), dropped abstract architect-speak. docs-plan-009 → -010. Doc-only.
- DOC 13: unified pattern/idiom catalog (patterns-009 → -010), merged api-reference pattern material, no repetition. docs-plan-010 → -011. Doc-only.
- DOC 14: audited sibling Odin project; added 7 catalog entries (Request-Response, Pipeline, Fan-In, Fan-Out, Shutdown via Exit message, Thread-is-container, Intrusive node embedding) to patterns-011. docs-plan-011 → -012. Doc-only.
- DOC 15: added `///`/`//!` doc comments to all of `src/*.zig`; lifted the src/ `///` ban (rules-010 → -011). Doc-only.
- DOC 16: banned-word + ownership-language cleanup pass on `src/*.zig` doc comments; rules-011 → -012. Doc-only.
- DOC 16b: gap-fix for 6 missed ownership hits; header staccato standard added; rules-012 → -013. Doc-only.
- DOC 17: snake_case entry points; fixed autodoc "Declaration not found" bug; rules-013 → -014. Doc-only.
- DOC 17b/17c: example doc comments moved to file-level `//!`; ASCII diagrams fenced; rules-014 → -015. Doc-only.
- DOC 18: humanized the API reference (-019 → -020), dropped "ownership" framing throughout. Doc-only.
- DOC 18b/18c: `//!` splice bug root-caused via headless-Chrome render; `_doc_stub` fix applied to mailbox/pool/polynode; rules-015 → -017. Doc-only.
- API 4: renamed `NodeHandle` → `ItemHandle` across src, examples, stories, design docs; documented `ih`/`handle` shorthand. 167/167 tests.
- API 4b: propagated the rename to `kitchen/docs/` site pages and regenerated autodocs.
- DOC 19: moved GitHub Pages generated site from `kitchen/output/` to root-level `docs/`.
- INTR 6: split standalone `helpers/` build module into `examples/items/` (4 item types +
  `items.zig` lifecycle helpers), `examples/hooks/` (`AlwaysCreateHooks.zig`,  
  `CappedPoolHooks.zig`, `hooks.zig`), `examples/helpers/` (generic `expect`/`clearList`  
  only); updated `build.zig` to remove the standalone `helpers` module and wire `smod` to  
  `examples`; updated ~68 call-site files; old `helpers/` folder deleted. 167/167 tests.
- DOC 20: removed the 8 example-autodoc `zig build docs` targets (`layer1docs`..`layer4docs`,
  `itemsdocs`, `hooksdocs`, `helpersdocs`, `storiesdocs`) and their unused `build.zig` helpers  
  (`addLayerDocTarget`, `stageDir`, `addDocTargetForModule`); `apidocs` (the real  
  `src/matryoshka.zig` API reference) untouched. Added a new permanent  
  `kitchen/tools/gen_examples_docs.sh` that mirrors `examples/`+`stories/` 1:1 into  
  `kitchen/docs/examples/` as generated `.md` (title, `//!` description + diagram verbatim,  
  embedded source, GitHub-blob "Open source" link); wired into `build_site.sh`/`preview_site.sh`.  
  Added 6 hand-authored catalog/group pages (Items/Hooks/Helpers intro + How-to PolyNode/  
  Mailbox/Pool/Io groups + Flow group for cross-layer Master compositions), replacing the  
  removed `examples_reference.md`. `.gitignore`/`kitchen/mkdocs.yml` updated to match.  
  rules-019 → -020 (Doc-generation module size rule updated). 167/167 tests.

---

## Next

Stage 9 — Docs + README + autodocs. PLANNED. Candidate: split  
`matryoshka-api-reference-019.md` into mkdocs Reference pages under  
`kitchen/docs/reference/`.

Open: owner to decide on removing old-named (pre-`NNN-`) layer1-3 example files, currently  
unreferenced and left in place.

Open (surfaced by INTR 6 doc pass): stale `helpers/`-path references found outside  
this task's scope — `design/patterns-012.md` (2 hits, `CappedPoolCtx`/`helpers/helpers.zig`  
examples), `design/matryoshka-api-reference-021.md` (3 hits), `design/collected-context-005.md`  
(historical, 3 hits), `kitchen/docs/patterns/pool.md` (2 hits), `kitchen/docs/api/pool.md`  
(1 hit). Owner to decide whether/when to update.

Open (DOC 20): the examples catalog's how-to/flow grouping is a first pass, owner-flagged as  
likely to be reshuffled later — regrouping is a pure doc edit (move links between group `.md`  
files), no script or generation change needed.
